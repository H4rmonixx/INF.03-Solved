Zapytanie 1: SELECT model FROM produkt;
Zapytanie 2: SELECT b.model, b.nazwa, b.cena, p.nazwa_pliku FROM buty b INNER JOIN produkt p ON b.model = p.model;
Zapytanie 3: SELECT b.nazwa, b.cena, p.kolor, p.kod_produktu, p.material, p.nazwa_pliku FROM buty b INNER JOIN produkt p ON b.model = p.model WHERE b.model LIKE 'P-59-03';
Zapytanie 4: INSERT INTO kategorie (nazwa_kat) VALUES ('Sandały');